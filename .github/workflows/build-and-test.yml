name: Build and Test

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:

      # Checks-out the repository
      - uses: actions/checkout@v2

      # Build the app
      - name: Build the Java modules
        uses: xlui/action-maven-cli/jdk11@master
        id: test
        with:
          lifecycle: 'clean package'